<% if current_user %>
	<%= link_to('Create goal', goals_path(goal: {title: "Test Goal", user_id: current_user.id}), :method => :post) %>

	<%= react_component('GoalForm', userId: current_user.id) %>

Myself:
	<%= current_user %>

	My goals:
	<% @goals.each do |goal| %>
		<%= react_component('Goal', title: goal.title, id: goal.id) %>
	<% end %>
<% else %>
	No current user. Please log in!
<% end %>
